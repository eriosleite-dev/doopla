# types

Tipos TypeScript de domínio do app mobile. Deliberadamente NÃO importa
nada de `../../src` do Next.js por caminho relativo (decisão do
usuário) — qualquer tipo compartilhado com o painel web será copiado
ou distribuído de outra forma numa etapa futura, nunca por import
relativo cruzando os dois projetos.
