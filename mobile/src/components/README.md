# components

Componentes de UI reutilizáveis do app mobile (React Native puro,
sem dependência do Next.js). Nenhum componente do layout aprovado foi
implementado ainda — esta pasta existe só como fundação de estrutura.
