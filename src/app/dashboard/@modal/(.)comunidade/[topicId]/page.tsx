// Reaproveita 100% da rota real (dados/Server Actions da Fase 1) — só
// o container muda (ver layout.tsx em @modal/(.)comunidade). Este é o
// estado que faz o painel expandir (ver isTopicDetail no layout).
export { default } from '../../../comunidade/[topicId]/page';
